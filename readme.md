### metric learning siamese nn implementation
##### blake mason, jack wolf

## how to run
- `pip install` dependencies
- `python3 manager.py path/to/input.yaml`

## input files
- set parameters via yaml files, check samples